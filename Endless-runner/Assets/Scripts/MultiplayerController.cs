using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Multiplayercontroller : MonoBehaviour
{
    // Start is called before the first frame update
    /*
    Do setup, double wide the terrain, set variable probably
    setup scoring
    change spawn rates
    allow second input
    */
    void Start()
    {
        
    }

    // Update is called once per frame
    /*
    Update both players scores
    check collisions (Potentially use functions in and instance the PlayerController.cs file?)
    */
    void Update()
    {
        
    }
}
